-content_for :stylesheets
  = stylesheet_link_tag "web-app-theme/base", "web-app-theme/override"
.content
  %h2.title
    Подать заявку на обучение
  .inner
    = form_for :person, :url => people_path, :html => { :class => :form } do |f|
      = render :partial => "form", :locals => {:f => f}
