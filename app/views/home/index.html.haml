#buttons
  %a.prev{:href=>"#"}Назад
  %a.next{:href=>"#"}Вперед
  %br.clear/
#slider
  %ul
    - @banners.each do |b|
      %li
        -if !b.url?
          = image_tag b.image_url
        -else
          = link_to image_tag (b.image_url) , b.url
