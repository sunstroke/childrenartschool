.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_programs_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_program_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_admin_program_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Program
    .inner
      = form_for @program, :url => admin_program_path(@program), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
