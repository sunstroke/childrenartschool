.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_payments_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_payment_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Payment
    .inner
      = form_for :payment, :url => admin_payments_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
